import { AppBar } from '@components/ui';
import { PageContent } from 'components/layout/page';

interface Props {}

const Home = (props: Props) => {
  return <PageContent>Hello World!</PageContent>;
};

export default Home;
